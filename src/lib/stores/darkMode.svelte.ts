/**
 * Backward-compatible re-export. Theme state lives in `themeStore.svelte.ts`
 * (light / dark / warm). Prefer importing `themeStore` or `darkModeStore` from there.
 */
export { darkModeStore } from './themeStore.svelte';
