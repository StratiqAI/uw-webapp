/**
 * Re-exports the ValidatedTopicStore implementation from `validatedTopicStore.svelte.ts`.
 *
 * @see {@link ../../../docs/validated-topic-store.md} — full guide including realtime / ontology integration (§13)
 */
export {
	validatedTopicStore,
	ValidatedTopicStore,
	type SchemaRegistration,
	type StoreChangeEvent
} from './validatedTopicStore.svelte';
