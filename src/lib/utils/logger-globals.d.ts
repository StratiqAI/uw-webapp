/**
 * Build-time constant injected by Vite `define`.
 * `true` in dev, `false` in production — enables dead-code elimination
 * of all logging paths by the minifier.
 */
declare const __LOG_ENABLED__: boolean;
