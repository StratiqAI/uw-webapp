import { describe, it } from 'vitest';

/**
 * Phase 2 (plan): streaming / tool behavior with mocked Vertex + AI SDK.
 * Skipped until shared pure helpers are extracted from routes and agents.
 */
describe.skip('Agent parity — streaming & tools (Phase 2)', () => {
	it('placeholder', () => {});
});
